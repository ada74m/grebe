<%= form_for(@ship) do |f| %>
  <% if @ship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ship.errors.count, "error") %> prohibited this ship from being saved:</h2>

      <ul>
      <% @ship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :dwt %><br />
    <%= f.text_field :dwt %>
  </div>
  <div class="field">
    <%= f.label :built_year %><br />
    <%= f.text_field :built_year %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
